# API-34 — Строгая валидация POST/PATCH

## Контекст
Для NFR-02 нужно гарантировать, что сервис принимает только данные, прошедшие схемы Pydantic. Сейчас часть проверок живёт в обработчиках и не покрыта тестами.

## Что делаем
- перенести валидацию в схемы `IdeaCreate`, `IdeaUpdate`, `EvaluationCreate`;
- обновить/добавить тесты в `tests/test_ideas.py`;
- описать пример недопустимого payload в README.

## Приёмка
1. `pytest -q` зелёный и содержит тесты на негативные кейсы.
2. `ruff` не ругается на неиспользуемые проверки.
3. BDD-сценарии для NFR-02 проходят на stage.

## Команда и сроки
- Ответственный: backend-team (А. Иванова).
- Спринт: Security Hardening Sprint 1 (до 2025-02-14).
